import React, { Component } from "react";
import { Typography } from "@material-ui/core";

class Checkout extends Component {
  constructor(props) {
    super(props);
    this.state = {};
  }
  render() {
    return <Typography variant="h4">Checkout Page WIP</Typography>;
  }
}

export default Checkout;
